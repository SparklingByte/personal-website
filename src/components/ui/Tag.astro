---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<div class="flex flex-wrap gap-2">
    <div
        class="bg-slate-100 border h-auto px-2 py-1 text-xs rounded-md text-black"
    >
        <p class="font-cascadia mr-1">{title}</p>
    </div>
</div>
